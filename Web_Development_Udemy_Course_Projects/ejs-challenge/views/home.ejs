
<%- include("partials/header"); -%>

  <h1>Home</h1>

  <p><%= homeText %></p>

  <!--
  <% for(var i=0; i < posts.length; i++) { %>
    <h1><%= posts[i].title %></h1>

    <p><%= posts[i].content %></p>
  <% } %>
  -->

  <% posts.forEach(function(post) { %>
    <h1><%= post.title %></h1>

    <p>
      <%= post.content.substring(0, 100) + " ... " %>
      <a class="read-more" href="/posts/<%= post.slug %>">Read More</a>
    </p>
    <!-- substring(start position, end position) -->

  <% }); %>

  <!-- The forEach() method is similar to for loop.
  array.forEach(function(element of the array) {}); -->

<%- include("partials/footer"); -%>
